{
	"name": "notion-to-google-chat-relay",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "vite build",
		"cf-typegen": "wrangler types",
		"check-types": "tsc --noEmit",
		"check:fix": "biome check --write",
		"check": "biome check",
		"deploy": "bun run build && wrangler deploy",
		"dev": "vite dev",
		"preview": "bun run build && vite preview",
		"start": "wrangler dev",
		"test": "vitest"
	},
	"dependencies": {
		"hono": "^4.12.30"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.3",
		"@cloudflare/vite-plugin": "^1.44.0",
		"@cloudflare/vitest-pool-workers": "^0.18.4",
		"@notionhq/client": "^5.23.0",
		"@types/node": "^26.1.1",
		"typescript": "^6.0.3",
		"vite": "^8.1.4",
		"vitest": "^4.1.10",
		"wrangler": "^4.110.0"
	}
}
